### grid_index.py --- Builds a grid index which divides the space into uniform cells 
# and assigns each point to a cell based on its coordinates

import pandas as pd

def build_grid_index(dataset: pd.DataFrame, cell_size: float):
    return None